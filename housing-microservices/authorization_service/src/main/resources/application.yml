server:
  port: 8085

spring:
  application:
    name: authorization-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/microservice_housing
    username: postgres
    password: postgres

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

token:
  invalidate:
    password: test123
